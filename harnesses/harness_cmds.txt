#----sample harness_cmds.txt----
# set breakpoints/watches here
# b snprintf
# commands
# silent
# printf "@0x%x snprintf(dst:0x%x, size:0x%x, fmt: %s, src: %.20s)\n", $ra, $a0, $a1, $a2, $sp+8
# continue
# end

set pagination off
set follow-fork-mode child
handle SIGPIPE nostop noprint nopass
run
############
# Cmd to run after crash
quit
